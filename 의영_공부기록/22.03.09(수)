def gcd(a,b): #a,b의 최대공약수를 먼저 구해보자
    while b:
        a,b = b,a%b
    return a
'%0.2f'%number

2. 1977_완전제곱수
 for문에서 시작점과 끝점의 차이가 없으면 시작점의 인자가 삽인되지 않음.


3. map 과 lambda
 lambda 인자 : 표현식 /// key=lambda x : x[0]
    여러인자 나열하기
    arr.sort(key = lambda x : (int(x[3]),int(x[2]),int(x[1])))
4. 특정문자 기준으로 나누기 ////  문자열.split( )
a=('13:52:30')
b=a.split(':')
print(b)
5.str형 정수형 자릿수 채우기
    5-1))  Str(정수).zfill(n) # n: 자릿수
    5-2))  print("%02d:%02d:%02d" % (h,m,s))
6. 피보나치
